# harness-flow disabled

本项目已按 Owner 要求禁用 project-local `harness-flow` extension。

Pi 会自动发现 `.pi/extensions/*/index.ts` 或 `index.js`；本目录中不保留这些入口文件。保留 `*.disabled` 仅作为历史源码证据，不作为可加载 extension。

重新启用必须由 Owner 明确授权为单独治理维护任务。
